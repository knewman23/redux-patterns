import React from "react";
import { TableCell, TableRow } from "@material-ui/core";

function Header() {
	return (
		<TableRow>
			<TableCell></TableCell>
			<TableCell></TableCell>
			<TableCell></TableCell>
			<TableCell></TableCell>
			<TableCell></TableCell>
			<TableCell></TableCell>
			<TableCell></TableCell>
			<TableCell></TableCell>
		</TableRow>
	);
}

export default function StudyTableHeader() {
	return <Header />;
}
